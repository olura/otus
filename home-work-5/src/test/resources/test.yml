spring:
  datasource:
    url: jdbc:h2:mem:testdb
  shell:
    interactive:
      enabled: false

  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: schema.sql
