spring:
  datasource:
    url: jdbc:h2:mem:testdb
  shell:
    interactive:
      enabled: false
  jpa:
    generate-ddl: false
    #generate-ddl: true
    hibernate:
      ddl-auto: none

  sql:
    init:
      mode: always
#      data-locations: data.sql
#      schema-locations: schema.sql
